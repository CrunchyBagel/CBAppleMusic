import XCTest
@testable import CBAppleMusicAPI
/*
final class DecodingTests: XCTestCase {

    func testLibraryPlaylistDecoder() throws {

        guard let data = json.data(using: .utf8) else {
            XCTFail("Unable to convert to data")
            return
        }

        let decoder = AppleMusicAPI.createDecoder()
        let response = try decoder.decode(AppleMusicAPI.LibraryPlaylistResponse.self, from: data)

        XCTAssertGreaterThan(response.data.count, 0)

//        for playlist in response.data {
//
//        }
    }

    public let json: String = """
"""
}
*/
